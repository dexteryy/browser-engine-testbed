# test case9:SVG Image embedding and citation methods

This directory contains 4 pages:

- `index.html`:Compare inline side by side / external citation / CSS Background three ways and provide simple batch rendering statistics.
- `inline.html`:Focus on inline style and window control.
- `external.html`:external focus SVG of img/object/embed Three references are isolated from the style.
- `css-background.html`:focus CSS background renderingofZoom, Positioning and repeating.

The unified style is located in `assets/style.css`;Test useofexternal SVG The file is `assets/example.svg`.

> Browser: Only the latest version is required Chromium.
